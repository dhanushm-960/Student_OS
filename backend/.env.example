# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# Replace with your actual local MongoDB connection string or Atlas cluster URI
MONGO_URI=mongodb://127.0.0.1:27017/studentos

# JWT Configuration
# Use a strong, unique secret key in production environments
JWT_SECRET=your_jwt_secret_key_change_me
JWT_EXPIRES_IN=7d
